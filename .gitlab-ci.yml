# Used to test and build on gitlab-ci
before_script:
    - /run.sh
    - composer install --quiet -o

phpunit:
    image: inet/sugarcrm:7.6.1.0-PRO
    script:
        - bin/phpunit --color=never
        - php -dphar.readonly=0 bin/compile
    cache:
        paths:
            - vendor
