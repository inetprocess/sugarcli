**Directory Description**
This directory should contain only the absolutely essential code customizations for the _{{ module }}_ module that cannot be handled via logics hooks. See directory `custom/modules/{{ module }}/` for logic hooks.