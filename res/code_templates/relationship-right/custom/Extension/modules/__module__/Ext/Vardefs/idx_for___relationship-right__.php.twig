<?php
/**
 * @author SugarCli
 * SugarCli created file
 */

// Relationship foreign key to {{ moduleLeft|replace({'_': ' '}) }} index
$dictionary["{{ moduleBean }}"]["indices"][] = array(
    // Basic index features
    'name' => 'idx_{{ moduleLeft|singularize|lower }}_id',
    'type' => 'index',
    'fields' => array(
        '{{ moduleLeft|singularize|lower }}_id'
    )
);