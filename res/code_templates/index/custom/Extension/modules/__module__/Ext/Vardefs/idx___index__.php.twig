<?php
/**
 * @author SugarCli
 * SugarCli created file
 */

// {{ index }} index definition
$dictionary["{{ moduleBean }}"]["indices"][] = array(
    // Basic index features
    'name' => 'idx_{{ index }}',
    'type' => 'index',
    'fields' => array(
        '{{ fields|replace({',': '\',\''}) }}'
    )
);